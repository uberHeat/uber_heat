http:
    routers:
        router-back:
            entryPoints:
                - web
            service: back
            rule: Host(`toto.localhost`)

    services:
        back:
            loadBalancer:
                servers:
                  - url: "http://nginx:80/"
