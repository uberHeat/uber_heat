http:
    routers:
        router-front:
            entryPoints:
                - web
            service: front
            rule: Host(`api.uberheat.localhost`)

    services:
        front:
            loadBalancer:
                servers:
                  - url: "http://nginx:80/"
